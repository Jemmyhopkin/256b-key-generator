# 256-bit Key Generator
This repository contains a script for generating a random 256-bit key.

## Usage
Run the script with Python:
git add generate_key.py
git commit -m "Add 256-bit key generator script"
git push origin main
python generate_key.py
